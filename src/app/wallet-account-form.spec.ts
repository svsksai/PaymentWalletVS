import { WalletAccountForm } from './wallet-account-form';

describe('WalletAccountForm', () => {
  it('should create an instance', () => {
    expect(new WalletAccountForm()).toBeTruthy();
  });
});
